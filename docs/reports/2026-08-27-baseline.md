# Báo cáo Baseline — 2026-08-27

## 1. Kết quả kiểm tra 4 bước

| Bước         | Lệnh                             | Kết quả | Chi tiết                                                 |
| ------------ | -------------------------------- | ------- | -------------------------------------------------------- |
| 1. Cài đặt   | `pnpm install --frozen-lockfile` | ✅ XANH | 721 packages, 3.5s                                       |
| 2. Lint      | `pnpm lint`                      | ✅ XANH | 0 errors, 6 warnings (react-refresh, exhaustive-deps)    |
| 3. Typecheck | `pnpm -r typecheck`              | ✅ XANH | 4/5 workspace projects (shared, notifications, web, api) |
| 4. Test      | `pnpm test`                      | ✅ XANH | **87 file, 1285 test**, 0 failed, 0 skipped, 284.71s     |
| 5. Build     | `pnpm -r build`                  | ✅ XANH | web (vite build OK), api (tsc OK)                        |

> Baseline xanh 100% ngay từ mốc ban đầu. **Không cần sửa bất kỳ lỗi nào.**

## 2. Kiểm kê hạ tầng test

### 2.1. Cấu hình chung (`vitest.workspace.ts`)

4 workspace project:

- **api**: `apps/api/src/**/*.test.ts`, biến môi trường test (DATABASE_URL giữ chỗ, JWT secrets, BCRYPT_ROUNDS=4), timeout 30s cho PGlite
- **web**: `apps/web/src/**/*.test.{ts,tsx}`
- **shared**: `packages/shared/src/**/*.test.ts`
- **notifications**: `packages/notifications/src/**/*.test.ts`

### 2.2. Danh sách file integration test (`apps/api/src/__tests__/`)

| #   | File                                             | Phạm vi kiểm thử                                                          |
| --- | ------------------------------------------------ | ------------------------------------------------------------------------- |
| 1   | `audit.integration.test.ts`                      | Nhật ký kiểm toán (audit log), RBAC, append-only constraint               |
| 2   | `auth.integration.test.ts`                       | Đăng ký, đăng nhập, refresh, logout, rate limiting                        |
| 3   | `categories.integration.test.ts`                 | CRUD danh mục, phân cấp 2 tầng, reorder, ràng buộc xoá                    |
| 4   | `category-discounts.integration.test.ts`         | Chiết khấu theo danh mục, ưu tiên KH trực tiếp hơn nhóm, CHECK constraint |
| 5   | `crit-c1-sync-dedup.integration.test.ts`         | Chống trùng đơn ngoại tuyến (clientId dedup)                              |
| 6   | `crit-c3-return-duplicate.integration.test.ts`   | Chống trùng dòng trong phiếu trả hàng                                     |
| 7   | `crit-c4-owner-escalation.integration.test.ts`   | Chống leo thang quyền owner                                               |
| 8   | `customer-groups-pricing-fk.integration.test.ts` | FK nhóm KH với bảng giá                                                   |
| 9   | `customer-prices.integration.test.ts`            | Giá riêng theo KH, RBAC, chống trùng                                      |
| 10  | `dashboard.integration.test.ts`                  | Chỉ số bảng điều khiển (doanh thu, tồn kho, cảnh báo)                     |
| 11  | `debt-adjustments.integration.test.ts`           | Điều chỉnh công nợ, audit log, immutability                               |
| 12  | `inventory-transactions.integration.test.ts`     | Biến động kho, WAC, điều chỉnh tồn                                        |
| 13  | `logging.integration.test.ts`                    | Request ID middleware, X-Request-Id                                       |
| 14  | `notifications-emit.integration.test.ts`         | Endpoint phát sự kiện thông báo                                           |
| 15  | `notifications-events.integration.test.ts`       | Phát hiện đăng nhập bất thường, seed rules                                |
| 16  | `pos-debt.integration.test.ts`                   | Bán hàng ghi nợ POS, hạn mức nợ                                           |
| 17  | `price-lists-compare.integration.test.ts`        | So sánh nhiều bảng giá, margin                                            |
| 18  | `price-lists-extended.integration.test.ts`       | Bảng giá chuỗi, làm tròn                                                  |
| 19  | `price-lists.integration.test.ts`                | CRUD bảng giá, công thức, RBAC                                            |
| 20  | `product-history.integration.test.ts`            | Lịch sử nhập hàng                                                         |
| 21  | `product-variants.integration.test.ts`           | Biến thể sản phẩm (1 tới 2 thuộc tính)                                    |
| 22  | `products.integration.test.ts`                   | CRUD sản phẩm, SKU tự động, tìm kiếm                                      |
| 23  | `purchase-orders.integration.test.ts`            | Nhập hàng, công nợ NCC, WAC                                               |
| 24  | `receipts.integration.test.ts`                   | Phiếu thu nợ, FIFO, phân bổ chỉ định                                      |
| 25  | `reports.integration.test.ts`                    | Báo cáo tuổi nợ, tổng hợp nợ, xuất CSV                                    |
| 26  | `returns.integration.test.ts`                    | Trả hàng, hoàn tiền/cấn trừ nợ, hoàn kho                                  |
| 27  | `stock-checks.integration.test.ts`               | Kiểm kho, cân bằng tồn, RBAC                                              |
| 28  | `store.integration.test.ts`                      | Thông tin cửa hàng, logo, RBAC                                            |
| 29  | `supplier-payments.integration.test.ts`          | Phiếu chi NCC, giảm nợ NCC                                                |
| 30  | `suppliers.integration.test.ts`                  | CRUD nhà cung cấp, chống trùng                                            |
| 31  | `unit-conversions.integration.test.ts`           | Đơn vị quy đổi (max 3), RBAC                                              |
| 32  | `users.integration.test.ts`                      | Quản lý nhân viên, PIN, khoá tài khoản                                    |
| 33  | `volume-prices.integration.test.ts`              | Giá sỉ theo bậc số lượng                                                  |

### 2.3. Cơ chế `test-env.ts`

File: `apps/api/src/__tests__/helpers/test-env.ts`

- Dùng `PGlite` in-memory (không cần Postgres thật)
- `drizzle-orm/pglite` với casing `snake_case`
- Tự động chạy toàn bộ migration từ `apps/api/src/db/migrations/`
- Seed mặc định: 1 store, 3 user (owner/manager/staff) với password + PIN + JWT access token sẵn
- Hàm `close()` giải phóng PGlite
- Hàm `issueRefreshToken()` tạo refresh token

## 3. Cách viết integration test mới

### 3.1. Ví dụ tối thiểu

```typescript
import { beforeAll, describe, expect, it } from 'vitest'

import { createTestEnv, type TestEnv } from './helpers/test-env.js'
import { createProduct, createCustomer, createCompletedOrder } from './helpers/factories.js'

// Biến môi trường — bắt buộc trước khi import các module dùng env
beforeAll(() => {
  process.env.JWT_ACCESS_SECRET = 'test-access-secret-min-32-chars-please-change'
  process.env.JWT_REFRESH_SECRET = 'test-refresh-secret-min-32-chars-please-change'
  process.env.ACCESS_TOKEN_TTL_SECONDS = '900'
  process.env.REFRESH_TOKEN_TTL_SECONDS = '604800'
  process.env.BCRYPT_ROUNDS = '4'
  process.env.COOKIE_SECURE = 'false'
})

describe('Tên module', () => {
  let env: TestEnv

  // Mỗi test case có DB riêng (PGlite in-memory), hoàn toàn cô lập
  beforeEach(async () => {
    env = await createTestEnv()
  })

  afterEach(async () => {
    await env.close()
  })

  it('mô tả hành vi', async () => {
    // 1. Tạo dữ liệu mẫu bằng factory
    const prod = await createProduct(env, { sellingPrice: 200_000 })
    const cust = await createCustomer(env)

    // 2. Gọi route / service cần test
    const app = createXxxRoutes({ db: env.db })
    const res = await app.request('/xxx', {
      method: 'POST',
      headers: {
        ...env.owner.authHeader,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        /* payload */
      }),
    })

    // 3. Assert
    expect(res.status).toBe(200)
    const data = await res.json()
    expect(data.total).toBe(200_000)
  })
})
```

### 3.2. Quy ước

1. **File đặt tại** `apps/api/src/__tests__/<tên-module>.integration.test.ts`
2. **Luôn dùng `createTestEnv()`** — mỗi `beforeEach` tạo DB mới, cô lập hoàn toàn
3. **Dùng factory helpers** (`./helpers/factories.ts`) thay vì tự insert SQL
4. **3 user có sẵn**: `env.owner`, `env.manager`, `env.staff` (đã có `authHeader`)
5. **Timeout**: 30s mỗi test (cấu hình trong `vitest.workspace.ts`)
6. **Không cần `.env`**: biến môi trường test đã cấu hình trong workspace

### 3.3. Factory helpers có sẵn

| Factory                                                       | Tạo gì               | Override                                   |
| ------------------------------------------------------------- | -------------------- | ------------------------------------------ |
| `createStore(env, overrides?)`                                | Store mới            | name, address, phone...                    |
| `createUser(env, overrides?)`                                 | User + JWT token     | storeId, role, phone, pin                  |
| `createCustomer(env, overrides?)`                             | Khách hàng           | debtLimit, groupId, currentDebt            |
| `createProduct(env, overrides?)`                              | Sản phẩm             | sellingPrice, costPrice, sku, withVariants |
| `createVariant(env, productId, overrides?)`                   | Biến thể SP          | attribute1Name/Value, sku, sellingPrice    |
| `createUnitConversion(env, productId, overrides?)`            | Đơn vị quy đổi       | unit, conversionFactor, sellingPrice       |
| `createCompletedOrder(env, productId, overrides?)`            | Đơn hoàn tất (cash)  | customerId, items[], orderOverrides        |
| `createDebtOrder(env, productId, customerId, overrides?)`     | Đơn nợ + debt record | items[], orderOverrides                    |
| `createPartialReturn(env, productId, customerId, overrides?)` | Đơn trả một phần     | returnQuantity, reason, refundMethod       |
| `resetFactorySeq()`                                           | Reset bộ đếm unique  | —                                          |

## 4. Đã sửa gì

**Không sửa logic nghiệp vụ nào.** Baseline xanh ngay từ đầu.

Chỉ bổ sung:

- `apps/api/src/__tests__/helpers/factories.ts` — factory helpers dùng chung
- `apps/api/src/__tests__/factories.integration.test.ts` — 22 test tự kiểm factory

## 5. Kết quả verify cuối cùng (sau khi thêm factory)

| Bước                | Kết quả                             |
| ------------------- | ----------------------------------- |
| `pnpm lint`         | ✅ 0 errors, 6 warnings             |
| `pnpm -r typecheck` | ✅ XANH                             |
| `pnpm test`         | ✅ **88 file, 1307 test**, 0 failed |
| `pnpm -r build`     | ✅ XANH                             |

## 6. Rủi ro/ghi chú

- 6 lint warning (react-refresh, exhaustive-deps) là pre-existing, không ảnh hưởng runtime
- Test chạy ~250 tới 285s do mỗi test case khởi tạo PGlite riêng + migration — CI nên dùng pool forks hoặc chia nhóm
- Factory dùng bộ đếm module-level (`seq`), nếu chạy test song song trên cùng worker thì cần gọi `resetFactorySeq()` trong `beforeEach`
